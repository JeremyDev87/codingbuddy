# Architecture Map

> Auto-generated by `yarn wiki:generate`. Do not edit manually.

## Module Dependency Graph

```
agent → config, custom, keyword, rules, shared
analyzer → config, shared
checklist → shared
cli → analyzer, config, main, model, shared, tui
config → analyzer, shared
context → checklist, config, keyword, shared
diagnostic → config
keyword → agent, config, rules, shared, skill
mcp → agent, analyzer, checklist, cli, config, context, diagnostic, keyword, model, rules, shared, skill, state, tui
model → config
parallel-validation → mcp
rules → config, custom, keyword, shared
shared → rules
state → config, keyword
tui → rules
```

## Modules

### agent
Agent service and prompt building
- **Dependencies:** config, custom, keyword, rules, shared

### analyzer
Project analysis — conventions, code sampling, package analysis
- **Dependencies:** config, shared

### checklist
Checklist generation service
- **Dependencies:** shared

### cli
CLI entry point and TUI launcher
- **Dependencies:** analyzer, config, main, model, shared, tui

### config
Project configuration loading and diff detection
- **Dependencies:** analyzer, shared

### context
Context document persistence (docs/codingbuddy/context.md)
- **Dependencies:** checklist, config, keyword, shared

### custom
Custom project-specific extensions

### diagnostic
Diagnostic logging
- **Dependencies:** config

### keyword
PLAN/ACT/EVAL/AUTO mode detection and auto-execution
- **Dependencies:** agent, config, rules, shared, skill

### main

### mcp
MCP protocol handlers — tool dispatch via Chain of Responsibility
- **Dependencies:** agent, analyzer, checklist, cli, config, context, diagnostic, keyword, model, rules, shared, skill, state, tui

### model
Model resolution and constants
- **Dependencies:** config

### parallel-validation
- **Dependencies:** mcp

### rules
Rules file reading, agent/skill schema validation
- **Dependencies:** config, custom, keyword, shared

### shared
Common utilities (async, error, format, path, file, security, version)
- **Dependencies:** rules

### skill
Skill recommendation and trigger matching

### state
State persistence for modes (autopilot, ralph, etc.)
- **Dependencies:** config, keyword

### tui
React/Ink terminal UI dashboard
- **Dependencies:** rules

## File Distribution

| Module | Files |
|--------|-------|
| agent | 6 |
| analyzer | 11 |
| checklist | 5 |
| cli | 40 |
| config | 8 |
| context | 9 |
| custom | 4 |
| diagnostic | 4 |
| keyword | 39 |
| mcp | 20 |
| model | 5 |
| parallel-validation | 6 |
| rules | 6 |
| shared | 23 |
| skill | 9 |
| state | 4 |
| tui | 76 |
